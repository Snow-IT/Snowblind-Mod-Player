<Page x:Class="SnowblindModPlayer.Pages.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="LogsPage">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" MinWidth="260"/>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Links: Dateiliste mit eigenem Scrollbereich -->
        <Grid Grid.Column="0" MinWidth="260">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- reserver Platz für Titlebar-Buttons -->
                <RowDefinition Height="6"/>
                <RowDefinition Height="*"/>  <!-- Dateiliste: scrollfähig -->
            </Grid.RowDefinitions>

            <ListBox x:Name="LogList" Grid.Row="2" HorizontalContentAlignment="Stretch"
                     SelectionChanged="LogList_SelectionChanged"
                     DisplayMemberPath="Name"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"/>
        </Grid>

        <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch"/>

        <!-- Rechts: Lesebereich mit eigenem Scrollbereich; Zeilenumbruch aktiviert -->
        <Border Grid.Column="2" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" Padding="6" MinWidth="200">
            <TextBox x:Name="LogContent" FontFamily="Consolas" FontSize="12"
                     VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                     TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True"/>
        </Border>
    </Grid>
</Page>